// index.js
import { registerRootComponent } from 'expo';
import App from './App';

// This registers your App component as the root of the app.
// Expo will take care of setting up AppRegistry whether you're using Expo Go or a standalone build.
registerRootComponent(App);
